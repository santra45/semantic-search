.env
__pycache__
*.pyc
*/__pycache__
/chroma_db_cloud_2
/chroma_db_local
/.agents
/.devin
/local_models
/venv
/logs
/init
# Admin console build output — produced on the server by the
# admin-ui-build compose service, never committed. Committing it would mean
# every build shows as a diff of minified bundles and the tree could disagree
# with what is actually deployed.
backend/app/static/admin/
admin-ui/node_modules/

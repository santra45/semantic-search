"""
Deprecated — CSV/PDF exports are produced by the Magento module from its own
chat-history tables. Router kept as an empty placeholder for import safety.
"""

from fastapi import APIRouter

router = APIRouter()

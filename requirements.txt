# Core Web Framework
# Bumped off 0.104.1 (which dragged in starlette 0.27 → anyio<4) so the modern
# google-genai SDK (ThinkingConfig.thinking_budget, needs anyio>=4.8) can
# install — see the AI/Embeddings block below. Matches the versions already
# resolved on the dev environment.
fastapi==0.136.0
uvicorn[standard]==0.24.0
anyio==4.9.0

# Templates & Forms
jinja2==3.1.2
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.23
pymysql==1.1.0
psycopg[binary,pool]==3.2.9

# Authentication & Security
python-jose[cryptography]==3.3.0
cryptography>=41.0.0
pycryptodome

# Vector Database & Search
qdrant-client==1.17.0
# Sparse-vector (BM25) generation for hybrid search (Phase 2.2).
# fastembed bundles a small ONNX BM25 model — ~30MB on first import,
# CPU-only, no GPU needed. Used by app/services/sparse_embedder.py.
fastembed>=0.3,<0.4

# AI / Embeddings
# Pinned to a build whose ThinkingConfig carries `thinking_budget` so Gemini 2.5
# thinking can be disabled on the raw genai.Client rerank / completion paths.
# Requires anyio>=4.8 (hence the fastapi bump above).
google-genai==1.73.1
openai
anthropic
tiktoken

# LangChain / LangGraph multi-agent
langchain==0.3.30
langchain-core==0.3.86
langchain-google-genai==2.1.12
langchain-openai==0.2.10
langchain-anthropic==0.3.0
langgraph==0.2.53
langgraph-checkpoint-postgres==2.0.8
numpy==1.26.4
beautifulsoup4==4.12.3

# Server-side export (chat history PDF)
weasyprint==62.3

# Data Validation
pydantic==2.9.2

# Caching
redis==5.0.1

# HTTP Client
requests==2.31.0
aiohttp==3.9.1

# Environment Management
python-dotenv==1.0.0

# Development & Testing (optional but useful)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
pydantic[email]